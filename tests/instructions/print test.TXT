print

/// Програмата ще изведе, че трябва първо да бъде отворен файл преди да може да се принтира съдържанието

open ..\tests\original files\print.txt

print jdfhjsd
print 1245
print "fsghsfg"

/// Всичките тези опити за принтиране са невалидни и програмата ще изведе съобщение, че "print" не приема аргументи

print

/// Изходът е форматиран по такъв начин, че на всеки ред е отделено еднакво количество място за всеки от стълбовете, като обикновено се заделя точно количество необходимо място, но в зависимост от стойностите на реалните числа може да се отдели малко повече от необходимото
/// Стойността на формулите се изчислява и се принтира реално число, ако се получи резултат. Ако се достигне до неопределеност като деление на 0, то се извежда "ERROR"
/// Текстовете са подравнени отляво, докато числата са подравнени отдясно
/// Празните клетки също се отпечатват като програмата отпечатва най-малкия правоъгълник, в който е заградена "съществената" част от таблицата (изрично декларирани празни клетки също са "съществени")

close
exit