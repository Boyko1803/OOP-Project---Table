open ..\tests\original files\complex formulas.txt
print

/// Първите три реда показват приоритета на операциите. Също така степенуването '^' има дясна асоциативност за разлика от останалите 4 операции. Може да се провери, че резултатите са верни.

/// Следващите три непразни реда показват промяната на проеритета чрез скобите. Може да се провери, че резултатите са верни.

/// Следващите три непразни реда показват извличането на стойност от клетки, които не са формули или са извън рамките на дефинираната таблица.
/// Ако са записани числа, то програмата директно взима стойността им (клетка R11C5).
/// Ако са записани числа под формата на текст, то програмата ги разпознава (клетки R11C2 и R11C3). В противен случай се взима стойност 0 (клетки R11C1 и R11C4).
/// Също така се взима стoйност 0 за клетки извън дефинираната таблица (клетка R100C1). Може да се провери, че резултатите са верни.

/// Следващите пет непразни реда показва извличането на стойност от клетки, които са формули.
/// При циклично извикване (R16C2 -> R17C1 -> R18C2 -> RR17C3 -> R16C2) участващите клетки стават неопределени.
/// При извикване на клетка на себеси си, тя става неопределена (например в R20C1).
/// При извикване на неопределена клетка (например в R17C4), то текущата клетка също става неопределена, въпреки че може останалите параметри да са наред.
/// При извикване на клетки с определена стойност, то изчислението си протича по нормалните правила (например в R16C5).
/// Дори да няма проблем с извличането на стойности от повиканите клетки, пак може да се получи деление на 0 или повдигане на 0 на отрицателна степен в пресмятането на стойността на текущата клетка (например в C19C5). Тогава клетката е неопределена и съответно всички клетки, които я извикват също са неопределени (например R18C5).
/// Програмата извежда очаквания резултат в примера. По принцип програмата използва вариант на DFS за обхождане на клетките при изчислението на стойността им, така че няма теоретично ограничение върху това колко навързани може да са извикванията помежду им, за да се изясни кои клетки са неопределени.
